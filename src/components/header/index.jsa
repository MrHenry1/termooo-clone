import React from 'react'

import { HeaderContainer, Title, IconBox, Icon } from './style.js'

const Header = () => {
  
  return (
    <HeaderContainer>
      <IconBox
        whileTap={{ scale: 1.1, rotate: '180deg' }}
        transition={{ type: 'spring', duration: 0.6, stiffness: 100}}
      >
        <Icon> A </Icon>
      </IconBox>
      <Title> Termu </Title>
      <IconBox
        whileTap = { { scale: 1.1, rotate: '-180deg' } }
        transition = { { type: 'spring', duration: 0.6, stiffness: 100 } }
      >
        <Icon> Z </Icon>
      </IconBox>
    </HeaderContainer>
  )
}

export { Header }
